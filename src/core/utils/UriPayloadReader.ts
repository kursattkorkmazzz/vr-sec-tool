import Logger from "./Logger";

/**
 * Reads the payload from the uri from a instance of class.
 *
 * @param {Il2Cpp.Object} instance - The instance object from which to read the payload from uri.
 * @returns {String} The payload data as a string, or empty if error happened.
 */
export default function UriPayloadReader(instance: Il2Cpp.Object): String {
  //console.log(instance.tryField("m_Uri")!.modifier);

  return "";
}
